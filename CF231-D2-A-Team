#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n; cin>>n;
    int p,v,t;
    int cnt=0;
    for(int i = 0 ; i < n ; i++)
    {
      cin >>p>>v>>t;
      if(p+v+t>=2)
        cnt++;

    }
    cout <<cnt<<endl;

    return 0;
}
